---
title: Gatsby
description: Start using Wedges with Gatsby.
breadcrumbs:
  - Installation
---

<Steps>

### Create Gatsby Project

To set up a new project, run the following command in your terminal:

```bash
npm init gatsby
```

### Configure Project

The system will prompt you with several configuration queries:

```txt showLineNumbers
What would you like to call your site?
- My Gatsby Project

What would you like to name the folder where your site will be created?
- ./my-gatsby-project

Will you be using JavaScript or TypeScript?
- Typescript

Will you be using a CMS?
- Choose your answer

Would you like to install a styling system?
- Tailwind CSS

Would you like to install additional features with other plugins?
- Choose your answer

Shall we do this?
- Yes
```

### Install the Package

<Tabs variant='underlined' defaultValue='pnpm'>
  <Tabs.List>
    <Tabs.Trigger value="pnpm">pnpm</Tabs.Trigger>
    <Tabs.Trigger value="npm">npm</Tabs.Trigger>
    <Tabs.Trigger value="yarn">yarn</Tabs.Trigger>
  </Tabs.List>

    <Tabs.Content value="pnpm">

```bash
pnpm add @lmsqueezy/wedges
```

    </Tabs.Content>

    <Tabs.Content value="npm">

```bash
npm i @lmsqueezy/wedges
```

    </Tabs.Content>

    <Tabs.Content value="yarn">

```bash
yarn add @lmsqueezy/wedges
```

    </Tabs.Content>

</Tabs>

### Configure Tailwind CSS

Update your `tailwind.config` file to add the necessary configuration for Wedges:

```tsx {1,8,13-14} title="tailwind.config.js" showLineNumbers
const { wedgesTW } = require("@lmsqueezy/wedges");

/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    `./src/pages/**/.{js,jsx,ts,tsx}`,
    `./src/components/**/*.{js,jsx,ts,tsx}`,
    "node_modules/@lmsqueezy/wedges/dist/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {},
  },
  darkMode: "class",
  plugins: [wedgesTW()],
}
```

### Using the Components

That's all. You can now start using the components in your application.

```tsx {1,4} showLineNumbers
import { Alert } from "@lmsqueezy/wedges";

export default function ExampleComponent() {
  return <Alert>You're awesome!</Alert>;
}
```

</Steps>
